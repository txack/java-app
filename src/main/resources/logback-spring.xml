<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="unknown"/>

    <!-- Console appender with trace_id/span_id for Loki correlation -->
    <!-- NOTE: OTel Java Agent injects MDC keys as trace_id / span_id (with underscore) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - trace_id=%X{trace_id:-} span_id=%X{span_id:-} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JSON appender for structured logging (Loki label extraction) -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${appName}"}</customFields>
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
        </encoder>
    </appender>

    <!-- Use JSON in production, CONSOLE in development -->
    <springProfile name="production,pre,pro">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="default,dev,des,local">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.idp.app" level="DEBUG"/>
    </springProfile>

</configuration>
